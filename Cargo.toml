[package]
name = "memory"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.45.1", features = [
    "rt-multi-thread",
    "macros",
    "net",
] }
serde_json = "1.0"
axum = "0.7"
chrono = "0.4"
